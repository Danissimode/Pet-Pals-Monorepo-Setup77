{
  "name": "pet-pals",
  "version": "1.0.0",
  "description": "Monorepo for Pet Pals project",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "test": "turbo run test"
  },
  "keywords": [
    "monorepo",
    "react-native",
    "expo",
    "tailwind",
    "nativewind"
  ],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.13.4",
    "prettier": "^3.5.3"
  },
  "dependencies": {
    "nativewind": "^4.1.23"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "react-native-web"
      ]
    }
  }
}
